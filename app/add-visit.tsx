import { View } from 'react-native';
import AddVisitScreen from '../components/AddVisitScreen';

export default function AddVisit() {
  return (
    <View style={{ flex: 1 }}>
      <AddVisitScreen />
    </View>
  );
} 